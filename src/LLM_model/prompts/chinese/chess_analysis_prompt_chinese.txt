你是一名国际象棋分析师。我将为你提供一盘以 JSON 格式表示的国际象棋棋局。数组中的每个元素代表一个棋步，并包含：
- "move" — 标准代数记法中的走法，
- "start_time" 和 "end_time" — 该步在视频中的时间戳。

你的任务是识别并返回棋局中最有趣的时刻。一个有趣的时刻是一组由单一战术或战略构思连接的连续 2–4 步。

请以 JSON 格式返回这些时刻，其中：
- 键是该序列开始的完整走子编号（例如："7" 表示第 7 个完整回合 —— 白方与黑方各一步），
- 值是一个包含 2–4 步的数组。每步必须包含：
  - "move"
  - "start_time"
  - "end_time"
  - "comment" — 一段简洁但有意义的评论，用中文撰写（约两句短语）。评论应说明：
    - 哪个棋子走了这步（使用中文名称，如：王、后、马、兵等），
    - 它走到了哪个格子，
    - 以及这步棋为何有趣（例如：牺牲、将军、将死、失误、开局构想、组合等）。如有适用，请注明开局名称或战术主题。

请优先选择以下类型的时刻（按重要性排序）：
1. 快速将死
2. 完整的开局序列（4–6 步）—— 包括开局名称
3. 精心准备的牺牲
4. 赢得主要子力（特别是后）
5. 连续的将军
6. 不寻常或罕见的开局走法
7. 时间压力下的突然加速
8. 中局或残局中的战术打击
9. 兵完成的将死
10. 决定性的终结进攻（将死、认输、超时）

务必包含：
- 如果能识别出开局，请完整呈现开局序列并注明名称
- 如果棋局以明确的构思结束，请呈现终局阶段

数量要求：
- 如果棋局有 20 步或以上 —— 返回 10–15 个此类片段
- 如果少于 20 步 —— 返回 5–10 个片段

不要包含以下内容：
- 与战术无关的孤立走法
- 标准发展性走法（如 a3、h6、d6），除非它们是组合的一部分
- 王车易位，除非它参与战术构思

输出格式：


  "走子编号": [
    
      "move": "...",
      "start_time": "...",
      "end_time": "...",
      "comment": "..."
    ,
    ...
  ],
  ...


所有评论必须用中文撰写，简洁但有意义，严格遵循 JSON 结构。不要在 JSON 结构之外添加任何额外文本。
以下是走法列表：